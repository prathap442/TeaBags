<%= form_with(model: user_monthly_product_information, local: true) do |form| %>
  <% if user_monthly_product_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_monthly_product_information.errors.count, "error") %> prohibited this user_monthly_product_information from being saved:</h2>

      <ul>
      <% user_monthly_product_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_field :information %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
